<main>
  <h2 class="col s12">Welcome to your dashboard</h2>
  <div class="modal-content">
    <div class="row">
      
    </div>
  </div>
  <div class="modal-content">
    <form class="addPost-form" id="addPost-formId">
      <div class="row">
        <h3 class="col s12">Add New Post</h3>
        <div class="parent">
          <input
            class="input"
            id="post-name"
            type="name"
            name="post-name"
            placeholder="What's your post titled?"
          />
          <input
            class="input"
            id="description"
            type="text"
            name="description"
            placeholder="What's on your mind?"
          />
          <button id="addToDashboard" class="btn" type="submit">Add to your
            addToDashboard!</button>
        </div>
      </div>
    </form>
  </div>

  <div class="modal-content">
    <div class="row">
      <h1 class="col s12">Your Posts:</h1>
      <br>
      <br>
      <div class="edit-post">
        {{#each posts}}
        <div class="individualPost">
          <a href="/dashboard/post/{{this.id}}" >
          <h2 class="post_name">Post Name: {{this.post_name}}</h2> </a>
          <p class="description">{{this.description}}</p>
          <p class="timestamp">{{formatDate this.createdAt "dddd, MMMM Do YYYY, h:mm a"}}</p>
          
          <button class="editPost-btn btn" data-post_id="{{this.id}}" href="/edit/{{this.id}}">Edit</button> 
          <button class="deletePost-btn btn" id="deletePost-btn-{{this.id}}" data-post_id="{{this.id}}">Delete</button>
        </div>

        {{#each this.Comments}}
          <div>
            <p class="comment">{{this.description}}</p>
            <p class="comment-timestamp">{{formatDate this.createdAt "dddd, MMMM Do YYYY, h:mm a"}}</p>
          </div>
        {{/each}}

         <div>
          <form class="comment-form" id="commentOnPost">
            <label for="commentOnPost{{this.commentOnPost}}">Leave a Comment!</label>
            <textarea id="commentEntry{{this.id}}" placeholder="Write your comment here!"></textarea>
            <button type="submit" class="comment-btn btn" id="comment-btn-{{this.id}}" data-post_id="{{this.id}}" href="/comment/{{this.id}}">Comment</button>
            </form>
        </div>


        <div>
          <form class="edit-post-btn-form hide" id="editPostForm{{this.id}}">
            <label for="editPostName{{this.id}}">Post Name</label>
            <input type="text" id="editPostName{{this.id}}" value="{{this.name}}" placeholder="Don't forget to add a post name, or it'll come back blank!">
            <label for="editPostDescription{{this.id}}">Post Description</label>
            <textarea id="editPostDescription{{this.id}}">{{this.description}}</textarea>
            <button class='saveEdit-btn btn' id="saveEdit-btn-{{this.id}}" data-post_id="{{this.id}}">Save</button>
            <button class="cancelEdit-btn btn" id="cancelEdit-btn-{{this.id}}" data-post_id="{{this.id}}">Cancel</button>
          </form>
        </div>
        <br>
        <br>

      </div>
      {{/each}}
    </div>
  </div>

</main>
<script src="/js/add-post.js"></script>
<script src="/js/edit-post.js"></script>
<script src="/js/comment-post.js"></script>
<script src="/js/indiv-post.js"></script>